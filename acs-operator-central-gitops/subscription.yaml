apiVersion: apps.open-cluster-management.io/v1
kind: Subscription
metadata:
  annotations:
    apps.open-cluster-management.io/git-branch: demo2
    apps.open-cluster-management.io/git-path: acs-operator-central-gitops/manifests/
    apps.open-cluster-management.io/reconcile-option: merge
  labels:
    app: acs-gitops-app
  name: acs-subscription-hub
  namespace: openshift-gitops
spec:
  channel: openshift-gitops/channel-acs-gitops-app
  placement:
    placementRef:
      kind: PlacementRule
      name: hub-placementrule
