apiVersion: apps.open-cluster-management.io/v1
kind: Channel
metadata:
  name: helm-nginx
  namespace: channel-helm-nginx
spec:
    pathname: https://kubernetes.github.io/ingress-nginx
    type: HelmRepo